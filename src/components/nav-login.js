import React from "react";
import { Navbar } from "react-bootstrap";

const NavLogin = (props) => {
  return (
    <div>
      <Navbar bg="primary" variant="dark">
        <Navbar.Brand href="#home">CNRPS</Navbar.Brand>
      </Navbar>
    </div>
  );
};

export default NavLogin;
